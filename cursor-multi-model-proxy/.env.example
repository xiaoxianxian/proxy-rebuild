# Proxy Server Port
PORT=18794

# Encryption Key for API keys
# Leave empty to auto-generate a secure key at startup (stored in .encryption-key)
# Or set a custom key (must be at least 32 characters for full entropy)
ENCRYPTION_KEY=
